version: '3.8'
services:
  backend:
    build: .
    ports:
      - "3001:3001"
    environment:
      - DB_HOST=${DB_HOST:-localhost}
      - DB_USER=${DB_USER:-alumno}
      - DB_PASSWORD=${DB_PASSWORD:-alumno123}
      - DB_NAME=${DB_NAME:-tienda_perritos}
    restart: unless-stopped
